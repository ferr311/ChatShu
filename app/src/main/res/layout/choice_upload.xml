<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/bottom_sheet_height"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior"
    app:behavior_hideable = "true"
    app:behavior_peekHeight = "0dp"
    android:background="@color/accent"
    android:id="@+id/bottom_sheet_choice">
    
    <ImageView
        android:id="@+id/bottom_sheet_btn_attach_image"
        android:layout_width="@dimen/bottom_sheet_btn_width"
        android:layout_height="match_parent"
        android:src="@drawable/ic_attach_image"
        android:layout_margin="@dimen/app_margin"/>
    <ImageView
        android:id="@+id/bottom_sheet_btn_attach_file"
        android:layout_width="@dimen/bottom_sheet_btn_width"
        android:layout_height="match_parent"
        android:src="@drawable/ic_attach_file"
        android:layout_margin="@dimen/app_margin"/>

</LinearLayout>